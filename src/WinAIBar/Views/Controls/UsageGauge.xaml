<UserControl
    x:Class="WinAIBar.Views.Controls.UsageGauge"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <StackPanel HorizontalAlignment="Center" Spacing="4" Padding="8,8">
        <Grid Width="80" Height="80">
            <ProgressRing x:Name="Ring" IsIndeterminate="False" Maximum="1.0"
                          Width="80" Height="80" />
            <TextBlock x:Name="PercentText"
                       FontSize="16" FontWeight="Bold"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
        <TextBlock x:Name="LabelText"
                   HorizontalAlignment="Center" FontSize="12" FontWeight="SemiBold"
                   TextWrapping="Wrap" TextAlignment="Center" MaxWidth="100" />
        <TextBlock x:Name="SubtitleText"
                   HorizontalAlignment="Center" FontSize="11"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" TextAlignment="Center" MaxWidth="100"
                   Visibility="Collapsed" />
        <TextBlock x:Name="ResetTextBlock"
                   HorizontalAlignment="Center" FontSize="10"
                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                   TextWrapping="Wrap" TextAlignment="Center" MaxWidth="100"
                   Visibility="Collapsed" />
    </StackPanel>
</UserControl>
